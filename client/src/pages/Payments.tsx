import PagePlaceholder from "@/components/PagePlaceholder";

export default function Payments() {
  return (
    <PagePlaceholder
      title="Payments"
      description="Manage all your bill payments, recurring transfers, and payment history in one place."
    />
  );
}
