import PagePlaceholder from "@/components/PagePlaceholder";

export default function Save() {
  return (
    <PagePlaceholder
      title="Save"
      description="Discover high-yield savings options, certificates of deposit (CDs), and retirement savings plans."
    />
  );
}
